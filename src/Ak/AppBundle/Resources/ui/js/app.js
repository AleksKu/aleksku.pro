var AkAppBundle = AkAppBundle || {};

AkAppBundle = (function($, window, undefined) {

    var init;

    init = function() {
    };

    return {
        init: init
    };

}(jQuery, window));

$(function() {
    AkAppBundle.init();
});
